# Jenkins Agent Setup

## Agent Details

Node Name:
Agent1

Operating System:
Windows 11

Java Version:
OpenJDK 21

## Connection Method

The Jenkins agent was connected using Jenkins Remoting.

The agent runs using the Jenkins agent.jar file and connects securely to the Jenkins Controller.

## Purpose

The agent provides remote execution capability for Jenkins jobs and allows workload distribution from the controller.